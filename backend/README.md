# RaceVault API

The backend owns source metadata, ingestion orchestration, retrieval, filtering,
reranking, and citation contracts. Milestone 1 contains only the service
foundation and document registry.

